# -*- mode: snippet -*-
# name: k8s-service
# key: svc
# --
apiVersion: v1
kind: Service
metadata:
  name: ${1:name}
  namespace: ${2:default}
spec:
  selector:
    app: $1
  ports:
    - port: ${3:80}
      targetPort: ${4:8080}
      protocol: TCP
  type: ${5:ClusterIP}
$0
