[[file:logo.png]]

* Ratatoskr Emacs

A modular, Evil-based Emacs configuration built for speed and productivity.

** Features

- *Evil mode* with full Spacemacs-style =SPC= leader keybindings via =general.el=
- *Elpaca* package manager with lockfile support for reproducible builds
- *Modular architecture* — each concern lives in its own =lisp/init-*.el= file
- *Completion stack* — orderless + vertico + marginalia + consult + embark
- *LSP support* with =lsp-mode=, =lsp-ui=, =breadcrumb= header context, and =corfu= completion
- *Apheleia* format-on-save (lsp formatting disabled — apheleia owns all formatting)
- *Language support* — Python, Rust, Go, C++, CMake, Terraform, YAML, Ansible, Helm templates, Dockerfile, Markdown, Just, PKGBUILDs, Jupyter notebooks
- *Godot 4.x / GDExtension* game development (clangd, dap-lldb, CMake)
- *Org-mode* — org-roam, org-super-agenda, org-kanban, org-download, org-transclusion, ox-hugo (blog export), org-re-reveal (reveal.js slides)
- *Kubernetes* — kubel integration for cluster management
- *Git* — magit, diff-hl (gutter indicators), browse-at-remote
- *LLM tools* — gptel, ellama, aidermacs (Ollama), agent-shell
- *Snippets* — yasnippet with 63 custom snippets across 9 modes
- *Multiple cursors* via =evil-mc=
- *REST client* — restclient for =.http= files
- *Workspaces* via =persp-mode=
- *Dashboard* start page with agenda, org-roam stats, and git status
- *Resilient startup* — modules load inside =condition-case=; one broken module won't take down the rest

** Dependencies

*** Core

| Dependency | Purpose                                       |
|------------+-----------------------------------------------|
| Emacs 29+  | Tree-sitter support (tested with 30.x)        |
| =git=        | Elpaca clones all packages from source        |
| [[https://just.systems][just]]       | Command runner for everything in the justfile |

*** Build tools (native modules)

Some packages compile native code on first use and fail at startup if these
are missing:

| Dependency                   | Needed by                                     |
|------------------------------+-----------------------------------------------|
| C compiler (=gcc= or =clang=)    | =jinx=, =vterm=, tree-sitter grammars, native-comp |
| =pkg-config=                   | =jinx= module compilation                       |
| =enchant= + development headers | =jinx= spell checking (binary =enchant-2= must be on PATH) |
| =cmake=, =libtool=               | =vterm= terminal emulator                       |
| =hunspell-en-us= or =aspell-en=  | Spell-checking dictionaries used by enchant   |

*** CLI tools

| Dependency          | Purpose                              |
|---------------------+--------------------------------------|
| =ripgrep=             | Search (consult, projectile)         |
| =fd=                  | Fast file finding                    |
| =shfmt=               | Shell script formatting (apheleia)   |
| =editorconfig-core-c= | EditorConfig support                 |
| =git=                 | Local reveal.js checkout (SPC o p i)  |

*** Language servers, formatters & dev tools

| Dependency        | Purpose                                                     |
|-------------------+-------------------------------------------------------------|
| =pyright=           | Python LSP                                                  |
| =python-black=      | Python formatting (apheleia)                                |
| =python-pytest=     | Python testing                                              |
| =gopls=, =go-tools=   | Go LSP + tooling                                            |
| =gomodifytags=      | Go struct tag editing                                       |
| =delve=             | Go debugging (dap-mode)                                     |
| =rustup=            | Rust toolchain — nightly + =rust-analyzer=, =clippy=, =rustfmt= |
| =prettier=          | JS/TS/YAML/Markdown formatting (apheleia)                   |
| =terraform=         | Terraform tooling                                           |
| =kubectl=           | Kubernetes cluster management (kubel)                       |
| =docker=            | Docker integration                                          |
| =hugo=              | Blog export (ox-hugo)                                       |
| =claude-code-acp=   | agent-shell AI integration (AUR)                            |
| =jdk-openjdk=       | Java 11+ runtime for the Snowflake JDBC driver (ejc-sql)    |
| =leiningen=         | Boots the Clojure nREPL ejc-sql drives JDBC through         |

*** Reveal.js presentations

Slide export runs through =org-re-reveal= (=SPC o p=). reveal.js assets load
from a CDN by default, so no local install is needed while online.

Decks are ordinary org-roam nodes living in the flat roam root
(=rata-reveal-deck-dir=), so they get IDs, show up in =org-roam-node-find=, and
can link to the notes they synthesise. A deck is identified by its
=#+filetags: :presentation:= tag (=rata-reveal-deck-tag=), not by its location —
=SPC o p P= exports every tagged note by querying the roam DB, the same
technique =init-org.el= uses for =:hastodo:= agenda inclusion.

Exported HTML goes to =~/workspace/second-brain/presentations/=
(=rata-reveal-export-dir=), outside =org-roam-directory=, so generated files never
enter the note tree — the same split as =hugo/= vs =hugo/public/=.

To start a new deck, use the =presentation= org-roam capture template
(=SPC o r c= then =r=, or =SPC o p n=). To turn a note you already have into a
deck, open it and press =SPC o p a= — it adds the tag and the =#+REVEAL_*=
keywords in place, leaving your content alone.

To present offline, run =SPC o p i= once — it shallow-clones reveal.js to
=~/.local/share/reveal.js= — then =SPC o p r= to point exports at that copy.
=SPC o p v= serves the deck over =simple-httpd= on localhost, which speaker
notes need (they do not work over =file://=).

*** Org LaTeX math preview

Requires =latex= and =dvisvgm= on PATH. On Arch these come from:
=texlive-binextra texlive-latex texlive-latexrecommended texlive-latexextra
texlive-plaingeneric= (wrapfig/capt-of from latexextra, ulem from plaingeneric).

*** Fonts

Icons (modeline, dirvish, dashboard, corfu) require *Symbols Nerd Font Mono*:

#+begin_src sh
  just install-fonts
#+end_src

*** Installing

On Arch Linux everything above is installed by:

#+begin_src sh
  just install-deps
#+end_src

On Debian/Ubuntu, install the core and native-build dependencies with:

#+begin_src sh
  sudo apt install build-essential pkg-config enchant-2 libenchant-2-dev \
      hunspell-en-us cmake libtool-bin ripgrep fd-find
#+end_src

Language servers and formatters on non-Arch systems come from their own
ecosystems (=npm=, =pip=, =go install=, =rustup=) — install the ones you need for
the languages you use.

** Getting Started

*** Clone

#+begin_src sh
  git clone https://github.com/Celsuss/Ratatoskr-emacs.git ~/workspace/Ratatoskr-emacs
#+end_src

*** Run

#+begin_src sh
  just run
#+end_src

Or manually:

#+begin_src sh
  emacs --init-directory ~/workspace/Ratatoskr-emacs
#+end_src

The justfile auto-detects the Emacs binary (=/usr/bin/emacs=, snap, or PATH);
set =EMACS_BIN= to override it per machine.

*** First Run

On the first launch Elpaca will clone and build all packages — this takes a few minutes. Subsequent startups are fast.

To install icon fonts and tree-sitter grammars:

#+begin_src sh
  just install-fonts
  just install-grammars
#+end_src

** Commands

| Command               | Description                                  |
|-----------------------+----------------------------------------------|
| =just run=              | Launch Emacs with this config                |
| =just debug=            | Launch with =--debug-init= for backtraces      |
| =just cli=              | Launch in terminal mode (no GUI)             |
| =just clean=            | Delete compiled artifacts, packages, history |
| =just reset=            | Clean then run                               |
| =just check <file>=     | Byte-compile a file for syntax checking      |
| =just load <file>=      | Batch-load a module to verify it parses      |
| =just batch=            | Full batch init (loads all modules)           |
| =just test=             | Run all tests (lint + compile + startup)     |
| =just lint=             | Run convention linting (fast, no Emacs)      |
| =just compile=          | Byte-compile all =.el= files                   |
| =just todos=            | Find TODOs in =.el= files                      |
| =just lock=             | Generate elpaca lockfile                     |
| =just update=           | Update all packages and regenerate lockfile  |
| =just install-fonts=    | Install nerd-icons fonts (one-time)          |
| =just install-grammars= | Install tree-sitter grammars (one-time)      |
| =just install-deps=     | Install system dependencies (Arch Linux)     |
| =just install-hooks=    | Install git pre-commit hook (one-time)       |

** Architecture

#+begin_example
  early-init.el          Disable UI chrome, max GC threshold
       |
    init.el              Bootstrap elpaca, load modules
       |
  lisp/init-*.el         Feature modules (loaded in order)
#+end_example

*** Module Load Order

| Module          | Responsibility                                          |
|-----------------+---------------------------------------------------------|
| =init-pkg=        | Package archives and =use-package= defaults               |
| =init-system=     | no-littering, exec-path-from-shell, savehist, recentf   |
| =init-ui=         | Theme (gruvbox), line numbers, which-key, modeline      |
| =init-evil=       | Evil + evil-collection, general.el, winum               |
| =init-completion= | Orderless, vertico, marginalia, consult, embark         |
| =init-dev=        | LSP, corfu, flycheck, magit, projectile, vterm, dirvish |
| =init-lang=       | Tree-sitter, language-specific modes and formatters     |
| =init-k8s=        | Kubel for Kubernetes cluster management                 |
| =init-gamedev=    | Godot 4.x / GDExtension development                    |
| =init-snippets=   | Yasnippet + community snippets                          |
| =init-llm=        | gptel, ellama, aidermacs                                |
| =init-persp=      | Workspace management via persp-mode                     |
| =init-org=        | Org-roam, org-super-agenda, org-kanban, org-transclusion |
| =init-present=    | Reveal.js slide export from org (org-re-reveal)          |
| =init-dashboard=  | Start page with agenda, roam stats, git status          |

** Keybindings

All keybindings use =general.el= with =SPC= as the global leader in Evil normal/visual states.

| Prefix  | Group      |
|---------+------------|
| =SPC b=   | Buffers    |
| =SPC c=   | Compile    |
| =SPC d=   | Debug      |
| =SPC e=   | Errors     |
| =SPC f=   | Files      |
| =SPC g=   | Git        |
| =SPC h=   | Help       |
| =SPC j=   | Jump       |
| =SPC l=   | Layouts    |
| =SPC L=   | LSP        |
| =SPC n=   | Narrow     |
| =SPC o=   | Org        |
| =SPC p=   | Project    |
| =SPC q=   | Quit       |
| =SPC r=   | Register   |
| =SPC s=   | Search     |
| =SPC t=   | Toggle     |
| =SPC w=   | Window     |
| =SPC x=   | Text       |
| =SPC y=   | Yank       |
| =SPC ;=   | Comment    |
| =SPC a g= | Gamedev    |
| =SPC a i= | AI tools   |
| =SPC a k= | Kubernetes |

** Adding a New Module

1. Create =lisp/init-{name}.el= with =;;; -*- lexical-binding: t; -*-= header
2. Use =use-package= for all packages (=:ensure t= is the default)
3. End the file with =(provide 'init-{name})=
4. Add =(rata-load-module 'init-{name})= to =init.el=

** License

See [[file:LICENSE][LICENSE]].
